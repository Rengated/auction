/** Имя BullMQ-очереди жизненного цикла торгов. Вынесено отдельно, чтобы константу
 *  можно было импортировать без подтягивания тяжёлых зависимостей lifecycle.service
 *  (иначе возникает циклический импорт через TelegramService). */
export const AUCTION_QUEUE = 'auction';
